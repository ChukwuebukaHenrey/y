import React from 'react'
import { useEffect } from 'react'

const UseEffectProject = () => {
  return (
    <div>
      
    </div>
  )
}

export default UseEffectProject

//Pure functions are simple funcs that provides the same output for the same input.
//A side effect is anything that can alter the state of a component. i.e anything not related to the output of your component might or mightnot influence the output of your component
